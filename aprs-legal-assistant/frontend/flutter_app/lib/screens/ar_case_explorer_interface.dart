import 'package:flutter/material.dart';

/// Base interface for AR Case Explorer implementations
/// This allows for consistent implementation across different platforms
abstract class ARCaseExplorerScreenBase implements Widget {
  // This is an interface class that defines the contract for AR explorers
  // No implementation needed here, just the interface definition
}
